{
	"name": "backend",
	"scripts": {
		"test": "bun test",
		"dev": "bun run --hot src/index.ts",
		"migrate": "bun src/db/migrate.ts",
	},
	"dependencies": {
		"hono": "^4.8.4",
		"kysely": "^0.28.2",
		"pg": "^8.16.3"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@types/pg": "^8.15.4"
	}
}
