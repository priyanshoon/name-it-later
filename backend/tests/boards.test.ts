import { describe, test } from "bun:test";

describe("Testing boards endpoints", () => {
	test.todo("GET /boards should list me the boards");
	test.todo("POST /boards should return boards posted by admins");
	test.todo("UPDATE /boards/{id} should return updated boards");
	test.todo("GET /boards/{id}");
})
